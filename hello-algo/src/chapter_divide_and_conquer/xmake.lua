target("binary_search_recur")
    set_kind("binary")
    add_files("binary_search_recur.cpp")

target("build_tree")
    set_kind("binary")
    add_files("build_tree.cpp")

target("hanota")
    set_kind("binary")
    add_files("hanota.cpp")
