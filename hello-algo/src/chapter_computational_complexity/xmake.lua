target("iteration")
    set_kind("binary")
    add_files("iteration.cpp")

target("recursion")
    set_kind("binary")
    add_files("recursion.cpp")

target("space_complexity")
    set_kind("binary")
    add_files("space_complexity.cpp")

target("time_complexity")
    set_kind("binary")
    add_files("time_complexity.cpp")

target("worst_best_time_complexity")
    set_kind("binary")
    add_files("worst_best_time_complexity.cpp")
