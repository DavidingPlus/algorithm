target("binary_search")
    set_kind("binary")
    add_files("binary_search.cpp")

target("binary_search_insertion")
    set_kind("binary")
    add_files("binary_search_insertion.cpp")

target("binary_search_edge")
    set_kind("binary")
    add_files("binary_search_edge.cpp")

target("two_sum")
    set_kind("binary")
    add_files("two_sum.cpp")
