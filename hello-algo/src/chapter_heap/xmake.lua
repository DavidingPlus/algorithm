target("heap")
    set_kind("binary")
    add_files("heap.cpp")

target("my_heap")
    set_kind("binary")
    add_files("my_heap.cpp")

target("top_k")
    set_kind("binary")
    add_files("top_k.cpp")
