target("avl_tree")
    set_kind("binary")
    add_files("avl_tree.cpp")

target("binary_search_tree")
    set_kind("binary")
    add_files("binary_search_tree.cpp")

target("binary_tree")
    set_kind("binary")
    add_files("binary_tree.cpp")

target("binary_tree_bfs")
    set_kind("binary")
    add_files("binary_tree_bfs.cpp")

target("binary_tree_dfs")
    set_kind("binary")
    add_files("binary_tree_dfs.cpp")

target("array_binary_tree")
    set_kind("binary")
    add_files("array_binary_tree.cpp")
