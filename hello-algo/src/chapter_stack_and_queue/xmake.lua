target("array_deque")
    set_kind("binary")
    add_files("array_deque.cpp")

target("array_queue")
    set_kind("binary")
    add_files("array_queue.cpp")

target("array_stack")
    set_kind("binary")
    add_files("array_stack.cpp")

target("deque")
    set_kind("binary")
    add_files("deque.cpp")

target("linkedlist_deque")
    set_kind("binary")
    add_files("linkedlist_deque.cpp")

target("linkedlist_queue")
    set_kind("binary")
    add_files("linkedlist_queue.cpp")

target("linkedlist_stack")
    set_kind("binary")
    add_files("linkedlist_stack.cpp")

target("queue")
    set_kind("binary")
    add_files("queue.cpp")

target("stack")
    set_kind("binary")
    add_files("stack.cpp")
